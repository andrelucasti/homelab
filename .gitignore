# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Ficheiros de variaveis podem conter segredos (auth keys, tokens)
*.tfvars
*.tfvars.json
!example.tfvars

override.tf
override.tf.json
*_override.tf
*_override.tf.json

.terraformrc
terraform.rc

# Kubeconfig gerado (contem credencial cluster-admin do k3s)
kubeconfig.yaml
**/kubeconfig.yaml
*.kubeconfig
